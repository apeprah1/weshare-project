# WeShare Enterprise — Frontend (React + Vite)

## Setup
```bash
npm install
npm run dev
```
By default it expects the API at `http://localhost:8000`.
You can override with `.env`:
```
VITE_API_BASE=http://localhost:8000
```
